/node_modules
/.next
/.netlify
/out
/build
.DS_Store
*.pem

# Secrets: ignore all real env files, but keep the committed template.
.env*
!.env.example

next-env.d.ts
*.tsbuildinfo

# Local assistant/tooling config
.claude/
